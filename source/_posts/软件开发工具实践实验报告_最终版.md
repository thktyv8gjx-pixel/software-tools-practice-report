---
title: 软件开发工具实践实验报告
date: 2026-07-10 20:00:00
categories:
  - 实验报告
tags:
  - 软件开发工具
  - Hexo
  - Git
  - Linux
  - SSH
  - Nginx
author: 任垚橙
---
## 基本信息

| 项目 | 内容 |
| --- | --- |
| 学校 | 杭州电子科技大学 |
| 学院 | 计算机学院 |
| 专业班级 | 软件工程3班（25052713） |
| 学生姓名 | 任垚橙 |
| 学号 | 25051414 |
| 报告名称 | 软件开发工具实践实验报告 |
| 项目主题 | 基于Hexo、Git、Ubuntu Server与Nginx的静态站点开发和部署 |
| 完成日期 | 2026年7月10日 |

## 摘要

本实验以“从本地开发到Linux服务器部署”为目标场景，在Windows 11中搭建Node.js、npm、Git、Visual Studio Code和Hexo开发环境，使用Markdown完成个人实验报告静态站点；通过Git记录项目版本并同步至GitHub；在VirtualBox中安装Ubuntu Server 22.04.5 LTS，配置NAT与Host-Only双网卡、Linux用户与权限、Shell环境变量、OpenSSH远程管理和裸Git仓库；最终使用Nginx提供静态Web服务，并编写PowerShell部署脚本实现Hexo构建、SCP上传、rsync同步、HTTP状态验证与双远程仓库推送。报告重点讨论可复现环境、最小权限、网络职责分离、密钥认证、版本快照和自动部署等工程配置，同时基于官方技术文档提出由“虚拟机+手工同步”升级为“容器化+GitHub Actions持续部署”的可行方案。

**关键词：** 软件开发工具；Hexo；Git；VirtualBox；Linux；SSH；Nginx；自动化部署

## 目录

1. 实验目的
2. 实验环境
3. 实验过程
   - 实验一：Web开发环境
   - 实验二：代码编辑与管理
   - 实验三：虚拟机安装与配置
   - 实验四：Linux基本操作
   - 实验五：Linux账户与权限管理
   - 实验六：Linux环境配置
   - 实验七：远程管理
   - 实验八：Web软件部署
4. 实验结果
5. 知识总结
6. 出现问题及解决方案
7. 心得体会
8. 技术展望
9. 参考文献

# 考核要求映射

| **评分维度** | **任务书权重** | **本报告对应内容** |
|:---|:---|:---|
| 目标场景描述 | 20% | 第1章明确“Windows本地开发—Git版本管理—Ubuntu远程运维—Nginx部署”的闭环场景，并用总体架构图和双网卡拓扑图说明。 |
| 技术栈说明 | 30% | 第2章列出实际软硬件版本；第3章逐项说明Node.js、npm、Hexo、Git、VirtualBox、Linux、SSH、rsync、Nginx等工具的职责与协同关系。 |
| 重点配置讨论 | 30% | 第3章与第5章重点分析npm镜像、.gitignore、NAT+Host-Only、Netplan静态IP、2770权限、.bashrc、authorized_keys、Nginx站点配置及部署脚本。 |
| 专业表达与格式 | 20% | 全文采用统一标题、表格、代码块、图注和结果验证表；所有关键操作均有真实截图。 |
| 技术展望 | 单独评分 | 第8章给出多个替代工具、容器化持续部署方案、调研方法、风险与实施计划；第9章列出所采用的课程文件与官方技术文档。 |

# 1 实验目的

## 1.1 总体目标

本课程实验旨在综合掌握现代软件开发工具的安装、配置和协同使用方法，独立完成从内容编写、代码版本管理、虚拟化Linux环境构建、账户权限配置、远程运维到Web静态站点部署的全过程。通过连续性的综合任务，形成可复现、可追踪、可维护和可部署的软件工程实践能力。<sup>/[1-2/]</sup>

## 1.2 目标场景

本人的目标场景为：在Windows 11宿主机上使用VS Code编写Markdown实验报告，由Node.js、npm和Hexo生成静态HTML；使用Git在本地形成版本快照，并同时推送至GitHub与Ubuntu裸Git仓库；使用VirtualBox运行Ubuntu Server，通过NAT网卡连接互联网、通过Host-Only固定地址192.168.56.101接受SSH与HTTP访问；使用SCP和rsync同步文件，由Nginx监听80端口提供静态站点；最后通过PowerShell脚本串联清理、构建、上传、同步和验证步骤。

<img src=".//images/word-report/image1.png" style="width:6.29861in;height:2.65208in" />

图1 从本地开发到服务器部署的完整工具链

## 1.3 分项目标

1/. 完成Web开发环境安装并运行Hexo静态站点，理解运行时、包管理器、依赖清单和静态站点生成器的关系。

2/. 使用VS Code和Markdown编写结构化文档，掌握Git工作区、暂存区、本地仓库、远程仓库及提交历史。

3/. 使用VirtualBox安装Ubuntu Server，配置虚拟硬件、NAT与Host-Only双网卡以及固定IP。

4/. 掌握Linux文件导航、内容查看、软件包管理、资源查看等基本命令。

5/. 完成用户、用户组、Home目录、文件所有权和权限控制，理解最小权限原则。

6/. 配置用户级和系统级环境变量、别名、PATH及自定义命令，并验证重新登录后的持久性。

7/. 实现SSH远程登录、SCP传输、ED25519密钥认证、快捷主机配置和Ubuntu裸Git仓库。

8/. 安装配置Nginx，将Hexo静态文件部署至服务器，并通过自动化脚本和浏览器验证部署结果。

# 2 实验环境

## 2.1 硬件与宿主机环境

| **项目**     | **实际环境**                                         |
|:-------------|:-----------------------------------------------------|
| 宿主机       | Windows 11，x64架构，已启用CPU硬件虚拟化             |
| 处理器与内存 | 支持VirtualBox硬件虚拟化；虚拟机分配2 vCPU、2 GB内存 |
| 项目目录     | D:/SoftwareToolsPractice/report-site                 |
| 浏览器       | Microsoft Edge / Google Chrome                       |
| 网络         | 宿主机互联网连接 + VirtualBox Host-Only私有网络      |

## 2.2 软件版本与服务

| **类别** | **软件/服务** | **实际版本或配置** | **主要作用** |
|:---|:---|:---|:---|
| 编辑与构建 | Visual Studio Code | 1.128.0 | Markdown编辑、终端、Git与插件管理 |
| 运行时 | Node.js | v24.18.0 | 运行Hexo及其JavaScript依赖 |
| 包管理器 | npm | 11.16.0 | 安装依赖与管理镜像源 |
| 静态站点 | Hexo CLI | 4.3.2 | 将Markdown生成HTML/CSS/JS |
| 版本控制 | Git for Windows | 2.54.0.windows.1 | 版本提交、远程同步与协作 |
| 虚拟化 | Oracle VirtualBox | 7.x | 运行隔离的Ubuntu服务器 |
| 服务器系统 | Ubuntu Server | 22.04.5 LTS | 承载SSH、Git与Nginx |
| 远程管理 | OpenSSH Server | 22端口，服务active | 加密登录、SCP与Git传输 |
| Web服务 | Nginx | 1.18.0（Ubuntu） | 监听80端口并提供静态文件 |
| 同步工具 | rsync | Ubuntu软件包 | 增量同步public目录 |

## 2.3 网络拓扑与地址规划

<img src=".//images/word-report/image2.png" style="width:5.98819in;height:3.34583in" />

图2 NAT与Host-Only职责分离的虚拟网络拓扑

| **设备/接口** | **地址** | **用途** |
|:---|:---|:---|
| Ubuntu enp0s3（NAT） | 10.0.2.15/24 | 访问npm、apt、GitHub等互联网资源 |
| Ubuntu enp0s8（Host-Only） | 192.168.56.101/24 | SSH、SCP、Git、HTTP服务固定入口 |
| Windows Host-Only适配器 | 192.168.56.1/24 | 宿主机访问虚拟机 |
| Web站点 | http://192.168.56.101 | 最终部署结果验证 |
| GitHub仓库 | thktyv8gjx-pixel/software-tools-practice-report | 源码备份与远程版本管理 |

## 2.4 项目目录规划

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">D:/SoftwareToolsPractice/report-site<br />
├─ source/_posts/软件开发工具实践实验报告.md<br />
├─ source/images/ # 报告截图<br />
├─ themes/ # Hexo主题<br />
├─ tools/deploy.ps1 # 自动部署脚本<br />
├─ public/ # hexo generate生成目录<br />
├─ _config.yml<br />
├─ package.json<br />
└─ .gitignore</td>
</tr>
</tbody>
</table>

# 3 实验过程（图文并茂）

本章按照正式任务书中的八项实验组织。每项实验均以本人真实操作为依据，采用“步骤—命令/配置—截图—验证”的方式记录，重点说明工具职责、关键配置及其工程意义。

## 3.1 实验一：Web开发环境

### 3.1.1 环境检查与安装

首先在PowerShell中检查Node.js、npm、Git和VS Code是否可用。初始检查发现node命令无法识别，因此安装Node.js并确保Node.js runtime、npm package manager和Add to PATH组件启用。安装后重新打开终端，完成版本验证，并将npm镜像设置为npmmirror以改善依赖下载稳定性。<sup>/[3-5/]</sup>

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">node -v<br />
npm -v<br />
git --version<br />
code --version<br />
npm config set registry https://registry.npmmirror.com<br />
npm config get registry<br />
npm install -g hexo-cli<br />
hexo -v</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image3.png" style="width:6.0625in;height:3.78611in" />

图3 Node.js、npm、Git、VS Code、npm镜像和Hexo版本验证

### 3.1.2 初始化Hexo项目

在D盘建立独立实验工作区，使用hexo init创建report-site项目。项目根目录中的_config.yml负责站点配置，source保存Markdown内容，themes保存主题，package.json记录依赖，public由构建命令自动生成。使用VS Code打开整个目录，便于同时使用编辑器、终端和源代码管理功能。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">D:<br />
mkdir SoftwareToolsPractice<br />
cd SoftwareToolsPractice<br />
hexo init report-site<br />
cd report-site<br />
npm install<br />
code .</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image4.png" style="width:6.0625in;height:3.80139in" />

图4 VS Code中显示的Hexo项目目录结构

### 3.1.3 本地运行与文章创建

在项目目录执行hexo server后，终端监听localhost:4000。浏览器成功显示默认Hexo页面后，使用hexo new创建个人实验报告文章，在Front-matter中设置标题、分类和标签，并使用Markdown编写姓名、学号、实验成果等信息。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">hexo server<br />
# 浏览器访问 http://localhost:4000<br />
hexo new "软件开发工具实践实验报告"</td>
</tr>
</tbody>
</table>

| <img src=".//images/word-report/image5.png" style="width:2.85417in;height:1.70417in" /> | <img src=".//images/word-report/image6.png" style="width:2.85417in;height:1.78264in" /> |
|:---|:---|
| 图5 Hexo默认静态站点运行成功 | 图6 个人实验报告文章渲染结果 |

### 3.1.4 本实验结果

Web开发环境完成后，所有核心工具均可在PowerShell调用，Hexo能够从Markdown内容生成并预览静态网页，为后续版本管理、服务器部署和实验报告展示提供了统一项目载体。

## 3.2 实验二：代码编辑与管理

### 3.2.1 Markdown编辑与渲染

使用VS Code编辑实验报告Markdown文件，采用标题、列表、表格、引用、行内代码和代码块等语法。Hexo开发服务器监听文件变化，保存后即可在浏览器刷新查看渲染效果。Markdown纯文本便于Git比较差异，也能转换为HTML、PDF和静态站点。

<img src=".//images/word-report/image7.png" style="width:6.0625in;height:3.60833in" />

图7 实验二Markdown内容在Hexo中的渲染效果

### 3.2.2 Git身份配置与首次提交

配置Git全局用户名和邮箱后，在项目根目录初始化仓库。通过.gitignore排除node_modules、public、日志及临时文件，避免将可重新生成的依赖和构建产物提交到仓库。首次提交形成项目初始版本。<sup>/[6/]</sup>

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">git config --global user.name "任垚橙"<br />
git config --global user.email "个人邮箱"<br />
git init<br />
git add .<br />
git commit -m "项目初始化并完成Web开发环境配置"</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image8.png" style="width:6.0625in;height:3.79097in" />

图8 Git用户信息配置与项目状态

### 3.2.3 修改、暂存与第二次提交

修改_config.yml中的站点标题、作者、语言和时区，并补充实验二内容。执行git status查看工作区变化，使用git diff核对差异，再将修改放入暂存区并提交。此过程体现了“工作区→暂存区→本地仓库”的基本模型。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">git status<br />
git diff<br />
git add .<br />
git commit -m "完善网站配置并新增代码编辑与Git管理实验"<br />
git log --oneline</td>
</tr>
</tbody>
</table>

| <img src=".//images/word-report/image9.png" style="width:2.85417in;height:1.80486in" /> | <img src=".//images/word-report/image10.png" style="width:2.85417in;height:1.79653in" /> |
|:---|:---|
| 图9 两次有意义的Git提交记录 | 图10 Git Graph图形化版本历史 |

### 3.2.4 GitHub远程仓库

在GitHub创建空仓库software-tools-practice-report，将本地分支改名为main，添加origin并推送。远程仓库用于异地备份、代码审阅和后续自动化流程触发；提交历史与源码均可在仓库页面查看。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">git branch -M main<br />
git remote add origin https://github.com/thktyv8gjx-pixel/software-tools-practice-report.git<br />
git push -u origin main</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image11.png" style="width:6.0625in;height:3.78611in" />

图11 GitHub远程仓库中的Hexo项目文件

### 3.2.5 本实验结果

完成了VS Code/Markdown编辑、Git版本控制、图形化历史查看和GitHub远程同步。项目变更具有提交说明和可追溯历史，满足代码库配置与版本管理验收要求。

## 3.3 实验三：虚拟机安装与配置

### 3.3.1 创建虚拟机与硬件配置

安装VirtualBox后创建ubuntu-template虚拟机，挂载Ubuntu Server 22.04.5 LTS ISO，分配2个虚拟CPU、2 GB内存和动态扩展虚拟硬盘。安装时创建renyaocheng账户、设置主机名ubuntu-server，并勾选OpenSSH Server。<sup>/[7/]</sup>

<img src=".//images/word-report/image12.png" style="width:6.0625in;height:4.00556in" />

图12 Ubuntu Server虚拟机硬件配置

### 3.3.2 NAT与Host-Only双网卡

网卡1使用NAT，以VirtualBox地址转换方式访问互联网；网卡2使用Host-Only，在宿主机与虚拟机之间建立独立私网。双网卡设计将外网下载和实验服务入口分离，避免默认路由冲突，并使SSH和HTTP始终使用固定私网地址。

<img src=".//images/word-report/image13.png" style="width:6.0625in;height:4.12778in" />

图13 VirtualBox的NAT与Host-Only双网卡设置

### 3.3.3 Ubuntu启动与静态IP配置

安装完成后系统识别enp0s3和enp0s8。编辑/etc/netplan/50-cloud-init.yaml，使NAT网卡继续使用DHCP，Host-Only网卡固定为192.168.56.101/24。Netplan文件使用YAML格式，必须严格采用空格缩进，且Host-Only接口不配置默认网关。<sup>/[8-9/]</sup>

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">network:<br />
version: 2<br />
ethernets:<br />
enp0s3:<br />
dhcp4: true<br />
enp0s8:<br />
dhcp4: false<br />
addresses:<br />
- 192.168.56.101/24</td>
</tr>
</tbody>
</table>

| <img src=".//images/word-report/image14.png" style="width:2.85417in;height:2.48889in" /> | <img src=".//images/word-report/image15.png" style="width:2.85417in;height:2.57986in" /> |
|:---|:---|
| 图14 Ubuntu Server启动并识别网络接口 | 图15 静态IP和互联网连通性测试 |

### 3.3.4 SSH远程登录

Ubuntu中的OpenSSH服务处于active (running)状态并监听22端口。Windows先ping 192.168.56.101验证Host-Only连通性，再使用ssh renyaocheng@192.168.56.101登录，首次确认服务器指纹后成功进入远程Shell。<sup>/[10/]</sup>

<img src=".//images/word-report/image16.png" style="width:6.0625in;height:3.78611in" />

图16 Windows通过Host-Only网络远程登录Ubuntu

### 3.3.5 本实验结果

Ubuntu虚拟机、双网卡、固定IP和SSH服务均配置成功。该服务器环境为后续Linux管理、Git服务和Nginx部署提供了稳定基础。

## 3.4 实验四：Linux基本操作

### 3.4.1 系统和用户信息

通过SSH终端使用whoami、hostname、uname -a、date、pwd和ip -br addr查看当前用户、主机、内核、时间、路径和网卡。命令行输出表明当前用户为renyaocheng，主机为ubuntu-server，Host-Only地址为192.168.56.101/24。

<img src=".//images/word-report/image17.png" style="width:6.0625in;height:3.20139in" />

图17 Linux系统、用户与网络信息

### 3.4.2 文件导航和管理

在~/software-tools-lab/exp4中创建testdir和file1.txt，使用echo写入内容，使用cp复制、mv重命名、mkdir创建备份目录、ls -R递归查看。操作完成后使用rm删除实验副本并返回上级目录，形成安全、可控的命令练习范围。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">mkdir -p ~/software-tools-lab/exp4/testdir<br />
cd ~/software-tools-lab/exp4/testdir<br />
touch file1.txt<br />
echo "Software Development Tools Practice" &gt; file1.txt<br />
cp file1.txt file2.txt<br />
mv file2.txt renamed.txt<br />
mkdir backup<br />
cp file1.txt backup/<br />
ls -R</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image18.png" style="width:6.0625in;height:3.22153in" />

图18 文件创建、复制、重命名和目录递归查看

### 3.4.3 软件包管理

使用apt update更新软件包索引，再安装tree工具。apt update并不直接升级系统，而是刷新可用包和版本信息；apt install负责下载并安装软件及依赖。tree命令用于以层次结构检查实验目录。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">sudo apt update<br />
sudo apt install tree -y<br />
tree --version<br />
tree ~/software-tools-lab</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image19.png" style="width:6.0625in;height:3.78611in" />

图19 APT软件包索引更新与tree安装

### 3.4.4 资源和进程查看

使用df -h查看文件系统空间，free -h查看内存和交换空间，ps aux /| head查看进程。该组命令是服务器排障的基础，可用于判断磁盘不足、内存压力和异常进程。

<img src=".//images/word-report/image20.png" style="width:6.0625in;height:3.20139in" />

图20 磁盘、内存和进程信息

### 3.4.5 本实验结果

已掌握Linux目录导航、文件创建与删除、内容查看与统计、软件包安装以及资源状态检查，能够在远程Shell中完成日常基础管理。

## 3.5 实验五：Linux账户与权限管理

### 3.5.1 创建用户与Home目录

使用sudo adduser webadmin创建部署用户。系统自动生成UID、GID、/home/webadmin和/bin/bash登录Shell。通过id、getent passwd和ls -ld核对账户、主组和Home目录。

<img src=".//images/word-report/image21.png" style="width:6.0625in;height:3.76944in" />

图21 webadmin用户、UID/GID与Home目录创建结果

### 3.5.2 创建项目组和共享目录

创建webgroup，将renyaocheng和webadmin追加到该组，并建立/srv/software-tools-site作为共享网站目录。目录属主为root、属组为webgroup，权限设置为2770。数字2表示setgid，新建文件将继承webgroup；770表示所有者和组可读写进入，其他用户无权限。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">sudo groupadd webgroup<br />
sudo usermod -aG webgroup renyaocheng<br />
sudo usermod -aG webgroup webadmin<br />
sudo mkdir -p /srv/software-tools-site<br />
sudo chown root:webgroup /srv/software-tools-site<br />
sudo chmod 2770 /srv/software-tools-site<br />
ls -ld /srv/software-tools-site</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image22.png" style="width:6.0625in;height:3.78611in" />

图22 webgroup成员及共享目录2770权限

### 3.5.3 多用户读写验证

renyaocheng先在共享目录创建ren-file.txt，再通过su - webadmin切换账户，由webadmin读取前一文件并创建新文件。两个用户均能访问共享目录，且文件继承webgroup，实现了组级协作而不开放给无关用户。

<img src=".//images/word-report/image23.png" style="width:6.0625in;height:3.78611in" />

图23 webadmin访问共享目录并创建文件

### 3.5.4 脚本执行权限

创建deploy-test.sh后首次直接执行出现Permission denied，原因是文件仅有读写权限。执行chmod u+x增加所有者执行位后，权限由-rw-rw-r--变为-rwxrw-r--，脚本正常输出Deploy test successful。

<img src=".//images/word-report/image24.png" style="width:6.0625in;height:3.78611in" />

图24 脚本增加执行权限前后的对比

### 3.5.5 本实验结果

完成账户、用户组、Home目录、所有权和权限管理，理解rwx、chmod、chown、setgid及最小权限原则。

## 3.6 实验六：Linux环境配置

### 3.6.1 用户级环境变量与别名

备份~/.bashrc和~/.profile后，在.bashrc末尾设置PROJECT_HOME、SITE_HOME和PATH，并创建cproject、csite、ll、labip等别名。执行source ~/.bashrc后，变量和别名立即生效。用户级配置仅影响当前账户，适合保存个人路径与命令习惯。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">export PROJECT_HOME="$HOME/software-tools-lab"<br />
export SITE_HOME="/srv/software-tools-site"<br />
export PATH="$HOME/bin:$PATH"<br />
alias cproject='cd "$PROJECT_HOME"'<br />
alias csite='cd "$SITE_HOME"'<br />
alias ll='ls -alF'<br />
alias labip='ip -br addr'</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image25.png" style="width:6.0625in;height:3.78611in" />

图25 ~/.bashrc中的环境变量与命令别名

### 3.6.2 自定义命令

创建~/bin/labinfo脚本，输出当前用户、主机、项目目录、网站目录、系统时间和内核版本。由于~/bin已加入PATH，增加执行权限后可直接输入labinfo运行，不需要写完整路径。

<img src=".//images/word-report/image26.png" style="width:6.0625in;height:3.78611in" />

图26 自定义labinfo命令运行结果

### 3.6.3 系统级环境变量

在/etc/profile.d/software-tools.sh中设置SOFTWARE_TOOLS_COURSE和SHARED_SITE_DIR。将文件属主设为root、权限设为644，使所有用户登录时可读取，但只有管理员可修改。与直接修改/etc/profile相比，profile.d独立文件更易维护和回滚。

<img src=".//images/word-report/image27.png" style="width:6.0625in;height:3.78611in" />

图27 /etc/profile.d中的系统级环境变量

### 3.6.4 持久性验证

退出SSH并重新连接后，再次输出PROJECT_HOME、LAB_STAGE、SOFTWARE_TOOLS_COURSE并运行labinfo。结果表明用户级和系统级配置能够在新登录会话中持久生效，而不是仅存在于当前Shell进程。

<img src=".//images/word-report/image28.png" style="width:6.0625in;height:3.78611in" />

图28 重新登录后环境变量和自定义命令仍然有效

### 3.6.5 本实验结果

掌握了.bashrc、.profile、/etc/profile.d、PATH、环境变量、别名和可执行脚本的加载关系，能够按用户范围和系统范围管理Linux运行环境。

## 3.7 实验七：远程管理

### 3.7.1 SCP文件传输

在Windows创建测试文件，使用scp上传到/home/renyaocheng，再登录Ubuntu核对文件内容。SCP基于SSH加密通道，适合一次性文件传输；后续目录持续同步采用rsync以减少重复传输。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">scp D:/SoftwareToolsPractice/transfer/windows-upload.txt /<br />
renyaocheng@192.168.56.101:/home/renyaocheng/</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image29.png" style="width:6.0625in;height:3.78611in" />

图29 Windows通过SCP上传测试文件

### 3.7.2 ED25519密钥与免密登录

在Windows执行ssh-keygen -t ed25519生成私钥id_ed25519和公钥id_ed25519.pub。私钥仅保存在个人电脑，公钥追加到Ubuntu的~/.ssh/authorized_keys。服务器端.ssh目录权限设为700，authorized_keys设为600，避免OpenSSH因权限过宽拒绝使用密钥。

| <img src=".//images/word-report/image30.png" style="width:2.85417in;height:1.78264in" /> | <img src=".//images/word-report/image31.png" style="width:2.85417in;height:1.78264in" /> |
|:---|:---|
| 图30 Windows生成ED25519密钥对 | 图31 公钥权限与免密登录验证 |

### 3.7.3 SSH客户端快捷配置

在C:/Users/renyaocheng//ssh/config中建立Host ubuntu-lab，指定HostName、User、Port和IdentityFile。之后输入ssh ubuntu-lab即可直接连接，scp和Git远程地址也可复用该别名。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">Host ubuntu-lab<br />
HostName 192.168.56.101<br />
User renyaocheng<br />
Port 22<br />
IdentityFile C:/Users/renyaocheng/.ssh/id_ed25519<br />
ServerAliveInterval 60</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image32.png" style="width:6.0625in;height:3.78611in" />

图32 SSH客户端Config快捷免密登录

### 3.7.4 Ubuntu裸Git仓库

在/srv/git创建software-tools-practice.git裸仓库。裸仓库不含工作区，适合作为远程共享端。Windows项目保留GitHub的origin，同时添加labserver远程并推送main分支，再在服务器使用--git-dir查看提交记录。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;"># Ubuntu<br />
sudo mkdir -p /srv/git<br />
git init --bare --shared=group /srv/git/software-tools-practice.git<br />
# Windows<br />
git remote add labserver ubuntu-lab:/srv/git/software-tools-practice.git<br />
git push -u labserver main</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image33.png" style="width:6.0625in;height:3.78611in" />

图33 项目推送到Ubuntu裸Git仓库并验证提交记录

### 3.7.5 本实验结果

实现了SCP传输、SSH公钥认证、快捷主机配置和自建Git远程仓库。远程管理从“每次输入IP和密码”提升为可供脚本与Git复用的安全连接。

## 3.8 实验八：Web软件部署

### 3.8.1 安装并启动Nginx

在Ubuntu使用apt安装nginx和rsync，通过systemctl enable --now nginx启动并设置开机自启。Nginx状态为active (running)，说明服务器进程可用。

<img src=".//images/word-report/image34.png" style="width:6.0625in;height:3.78611in" />

图34 Nginx安装、开机自启与运行状态

### 3.8.2 配置静态站点

创建/etc/nginx/sites-available/software-tools-site，监听IPv4和IPv6的80端口，以/srv/software-tools-site为root，并使用try_files按URI查找静态资源。建立sites-enabled符号链接、删除默认站点，执行nginx -t确认语法正确后reload。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">server {<br />
listen 80;<br />
listen [::]:80;<br />
server_name _;<br />
root /srv/software-tools-site;<br />
index index.html;<br />
location / {<br />
try_files $uri $uri/ =404;<br />
}<br />
}</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image35.png" style="width:6.0625in;height:3.78611in" />

图35 Nginx站点配置语法检查成功

### 3.8.3 生成和同步静态网页

Windows执行hexo clean与hexo generate，public目录生成index.html、CSS、JavaScript、分类、标签和归档页面。随后使用SCP上传public目录，再用rsync -av --delete将内容同步到Nginx根目录。--delete保证目标目录与本次构建保持一致，但使用前必须确认源、目标路径，避免误删。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">hexo clean<br />
hexo generate<br />
scp -r ./public ubuntu-lab:/home/renyaocheng/<br />
ssh ubuntu-lab "rsync -av --delete ~/public/ /srv/software-tools-site/"</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image36.png" style="width:6.0625in;height:3.78611in" />

图36 Hexo生成public静态网页目录

### 3.8.4 浏览器访问与结果验证

在Ubuntu执行curl -I http://localhost得到HTTP/1.1 200 OK，在Windows浏览器访问http://192.168.56.101，成功显示个人实验报告网站。该结果证明Host-Only网络、Nginx配置、文件权限和静态资源路径全部正确。

| <img src=".//images/word-report/image37.png" style="width:2.85417in;height:1.6875in" /> | <img src=".//images/word-report/image38.png" style="width:2.85417in;height:1.78264in" /> |
|:---|:---|
| 图37 通过虚拟机IP访问实验报告站点 | 图38 Ubuntu服务器最终网站页面 |

### 3.8.5 自动部署脚本和双远程推送

在项目tools目录创建deploy.ps1，将清理、构建、远程清理、SCP上传、rsync发布和HTTP状态检查串成单一命令。脚本完成后提交Git，并同时推送GitHub origin与Ubuntu labserver，形成“源码双备份+站点自动发布”的闭环。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">$ErrorActionPreference = "Stop"<br />
hexo clean<br />
hexo generate<br />
ssh ubuntu-lab "rm -rf ~/public"<br />
scp -r ./public ubuntu-lab:/home/renyaocheng/<br />
ssh ubuntu-lab "rsync -av --delete ~/public/ /srv/software-tools-site/ &amp;&amp; curl -I http://localhost"<br />
Write-Host "Deployment completed: http://192.168.56.101"</td>
</tr>
</tbody>
</table>

<img src=".//images/word-report/image39.png" style="width:6.0625in;height:3.52292in" />

图39 自动部署完成并推送两个远程仓库

### 3.8.6 本实验结果

完成了从Markdown源码、Hexo构建、Git版本管理、SSH传输到Nginx发布的全过程。部署脚本可重复执行，服务器返回200状态，浏览器可访问完整站点。

# 4 实验结果

## 4.1 八项实验验证汇总

| **序号** | **验收项目** | **验证方式** | **实际结果** |
|:---|:---|:---|:---|
| 1 | Web开发环境 | 版本命令、localhost:4000 | Node/npm/Git/VS Code/Hexo正常，站点可访问 |
| 2 | 代码编辑与管理 | Markdown页面、git log、GitHub | 渲染正确，提交历史完整，远程推送成功 |
| 3 | 虚拟机安装与配置 | ip addr、ping、ssh | NAT与Host-Only可用，固定IP 192.168.56.101 |
| 4 | Linux基本操作 | 文件操作、apt、tree、资源命令 | 目录、文件、软件包与资源管理命令执行成功 |
| 5 | Linux账户与权限 | id、groups、ls -l、多用户写入 | webadmin/webgroup与共享目录权限有效 |
| 6 | Linux环境配置 | echo变量、type别名、重新登录 | 用户级和系统级环境配置持久生效 |
| 7 | 远程管理 | SCP、免密SSH、Git裸仓库 | 文件传输、快捷登录、双远程仓库均成功 |
| 8 | Web软件部署 | nginx -t、curl -I、浏览器 | 配置正确，HTTP 200，站点可通过IP访问 |

## 4.2 最终成果清单

- 个人Hexo静态实验报告站点：本地http://localhost:4000，服务器http://192.168.56.101。

- GitHub源码仓库：software-tools-practice-report，保留完整提交历史。

- Ubuntu裸Git仓库：/srv/git/software-tools-practice.git。

- Ubuntu服务器：NAT地址10.0.2.15，Host-Only固定地址192.168.56.101。

- 自动部署脚本：tools/deploy.ps1，可完成构建、上传、同步和HTTP检查。

- Linux配置成果：webadmin、webgroup、共享站点目录、用户级/系统级环境变量、SSH公钥认证。

## 4.3 实验成绩考核项目和内容

| **考核项目**     | **分值** | **考核内容**                             |
|:-----------------|:---------|:-----------------------------------------|
| 项目与代码库配置 | 10       | 项目配置、编辑器配置、Git配置等          |
| Web运行环境      | 10       | 语言包、包管理器、关键软件包及配置等     |
| Linux环境配置    | 10       | 系统profile配置、用户配置、环境变量等    |
| Linux账户配置    | 10       | 账户配置、Home配置、关键文件的权限配置等 |
| 远程管理配置     | 10       | SSH服务、端口配置、登录配置等            |
| Web站点部署      | 10       | 可运行的站点、基本功能、部署脚本         |

核查结论：上述六项考核内容均已完整覆盖。对应证据包括第3章八项实验的配置过程与真实截图、第4.1节的结果验证，以及第5章对关键工具和重点配置的归纳分析。

# 5 知识总结

## 5.1 核心工具与职责

| **工具** | **类别** | **项目中的职责** |
|:---|:---|:---|
| VS Code | 代码编辑器 | 编辑Markdown、配置文件和脚本，集成终端与Git |
| Node.js | JavaScript运行时 | 执行Hexo和插件 |
| npm | 包管理器 | 依据package.json/package-lock.json管理依赖 |
| Hexo | 静态站点生成器 | 将Markdown与主题编译为静态HTML/CSS/JS |
| Git/GitHub | 分布式版本控制 | 形成版本快照并实现远程备份 |
| VirtualBox | 虚拟化平台 | 隔离运行Ubuntu服务器和虚拟网络 |
| Ubuntu Server | Linux系统 | 承载用户、SSH、Git、rsync和Nginx |
| OpenSSH | 远程管理协议与服务 | 加密登录、密钥认证、SCP和Git传输 |
| rsync | 增量同步工具 | 高效同步构建产物并删除目标冗余文件 |
| Nginx | Web服务器 | 监听80端口，提供静态文件 |

## 5.2 重点配置及工程意义

| **重点配置** | **关键内容** | **工程意义** |
|:---|:---|:---|
| npm镜像 | registry=https://registry.npmmirror.com | 改善依赖下载可用性，但应保留切回官方源的能力 |
| .gitignore | 排除node_modules、public、日志 | 减少仓库体积，避免提交可生成文件和临时数据 |
| NAT+Host-Only | 外网下载与固定服务地址分离 | 同时满足联网与可预测的SSH/HTTP入口 |
| Netplan静态IP | enp0s8=192.168.56.101/24 | 避免重启后地址变化，保证脚本和SSH Config稳定 |
| 2770+setgid | root:webgroup共享目录 | 组成员协作且不向其他用户开放；新文件继承项目组 |
| .bashrc与profile.d | 用户级和系统级变量分离 | 按作用范围管理环境，便于维护和排错 |
| authorized_keys权限 | ~/.ssh=700，authorized_keys=600 | 满足OpenSSH安全检查，保护密钥认证 |
| Nginx sites-available | 独立站点文件+符号链接 | 模块化配置，便于启停、检查和回滚 |
| 部署脚本 | clean→generate→scp→rsync→curl | 把人工步骤变为可重复流程，并以HTTP状态作为验收 |

## 5.3 关键原则

1/. 可复现：通过版本输出、依赖锁文件、配置文件和脚本记录环境，而不是依赖口头说明。

2/. 可追踪：每次提交聚焦一个目标，提交说明能够解释变更原因。

3/. 最小权限：不使用chmod -R 777解决问题，而是设计用户组、属主和必要权限。

4/. 职责分离：NAT负责外网，Host-Only负责实验内网；源码目录和生成目录分离；用户级与系统级配置分离。

5/. 先检查再生效：YAML、SSH和Nginx配置在重载服务前分别使用netplan generate、sshd -t和nginx -t检查。

6/. 自动化必须可验证：脚本不仅执行部署，还使用curl检查HTTP返回码，并保留Git提交记录。

# 6 出现问题及解决方案

实验过程中的错误并非额外负担，而是理解工具加载机制、环境变量、网络启动和配置格式的重要材料。以下问题均来自本人实际操作，并保留典型错误截图。

## 6.1 Node.js命令无法识别

执行node -v时PowerShell提示CommandNotFoundException。原因是Node.js尚未安装或安装目录未加入PATH。安装Node.js并保留Add to PATH，关闭旧终端后重新打开，node -v正常输出v24.18.0。

<img src=".//images/word-report/image40.jpeg" style="width:4.79306in;height:3.62153in" />

图40 典型问题 1 Node.js命令无法识别

## 6.2 PowerShell阻止npm.ps1

node可用但npm -v提示系统禁止运行脚本。原因是PowerShell执行策略阻止npm.ps1。执行Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned并重新打开终端后恢复；npm.cmd -v可用于确认npm本体是否安装。

<img src=".//images/word-report/image41.jpeg" style="width:4.89583in;height:3.62153in" />

图41 典型问题 2 PowerShell阻止npm.ps1

## 6.3 localhost页面拒绝连接

第一次将地址写成localhost4000，且Hexo服务已退出，浏览器显示ERR_CONNECTION_REFUSED。改为http://localhost:4000，并保持hexo server进程运行后解决。

<img src=".//images/word-report/image42.jpeg" style="width:2.77292in;height:3.62153in" />

图42 典型问题 3 localhost页面拒绝连接

## 6.4 虚拟机无法读取启动介质

VirtualBox启动时提示Could not read from the boot medium。原因是Ubuntu ISO未挂载。关闭虚拟机，在“设置-存储”中将ISO挂载到虚拟光驱并设置光驱优先启动。

<img src=".//images/word-report/image43.jpeg" style="width:4.12083in;height:3.62153in" />

图43 典型问题 4 虚拟机无法读取启动介质

## 6.5 Ubuntu安装在GRUB阶段失败

首次安装在curtin in-target/grub-pc阶段失败。删除失败虚拟机，重新创建动态VDI，关闭EFI与3D加速，使用完整Ubuntu Server 22.04.5 LTS镜像并避免安装阶段在线升级，第二次安装成功。

<img src=".//images/word-report/image44.jpeg" style="width:3.89653in;height:3.62153in" />

图44 典型问题 5 Ubuntu安装在GRUB阶段失败

## 6.6 Netplan YAML解析失败

netplan generate提示Invalid YAML: aliases are not supported，原因是误输入字符和缩进不规范。清空文件后使用空格重新输入，执行chmod 600、netplan generate和netplan apply，固定IP生效。

<img src=".//images/word-report/image45.jpeg" style="width:4.83611in;height:3.62153in" />

图45 典型问题 6 Netplan YAML解析失败

## 6.7 SSH快捷主机名无法解析

ssh ubuntu-lab提示Could not resolve hostname，原因是配置可能保存为config.txt或缺少Host行。使用PowerShell以ASCII编码重新写入~/.ssh/config，并通过ssh -G ubuntu-lab核对hostname、user、port和identityfile。

<img src=".//images/word-report/image46.jpeg" style="width:4.92083in;height:3.51181in" />

图46 典型问题 7 SSH快捷主机名无法解析

## 6.8 Hexo误加载PowerShell部署脚本

deploy.ps1放在Hexo根目录scripts文件夹后，Hexo将其当作JavaScript扩展加载并报SyntaxError。将脚本移到tools目录并重新运行，部署返回HTTP/1.1 200 OK。

<img src=".//images/word-report/image47.jpeg" style="width:4.15208in;height:3.62153in" />

图47 典型问题 8 Hexo误加载PowerShell部署脚本

# 7 心得体会

## 7.1 技术能力收获

本次实践最大的收获，不是简单记住若干命令，而是第一次真正把编辑器、构建工具、版本控制、虚拟机、Linux、网络、远程管理和Web服务器串联成一条完整的工程链路。过去我会把“写Markdown”“提交Git”“登录服务器”理解为互不相关的操作；经过本次实验后，我能够清楚说明每个工具的输入与输出：Markdown是内容源，Hexo将其构建为静态资源，Git保存可追踪版本，SSH承担安全传输，Linux权限保护部署目录，Nginx最终向浏览器提供服务。

这种从局部操作走向系统理解的变化，对我帮助很大。面对一个新项目时，我不再只关注“代码能不能运行”，而会主动确认运行环境、依赖版本、目录结构、网络入口、权限边界和部署验证方式。尤其是亲手完成本地localhost预览、GitHub远程备份、Ubuntu服务器发布和自动部署脚本后，我对“软件交付”有了更具体的认识：一个成果只有能够被别人复现、部署和验证，才真正具备工程价值。

排错过程也让我形成了更明确的方法：先观察错误原文和命令返回码，再判断问题属于路径、权限、服务、网络还是配置格式；修改后使用专用检查命令验证，而不是反复尝试。例如Netplan、SSH和Nginx分别有generate、sshd -t和nginx -t，部署完成还需要curl和浏览器双重确认。

## 7.2 工程思维与故障定位

实验中出现的多次故障，让我逐渐形成了较为清晰的排错思路：先完整阅读错误信息和命令返回结果，再判断问题属于路径、权限、服务状态、网络连通性还是配置格式；修改之后使用专用检查命令验证，而不是继续盲目尝试。例如，Netplan、SSH和Nginx分别可以通过netplan generate、sshd -t和nginx -t进行语法检查，部署后还要通过curl与浏览器进行双重确认。

我也认识到，很多错误并不是工具本身复杂，而是操作时忽略了细节和约定。例如git参数前漏写空格、localhost地址漏写冒号、YAML缩进不规范、PowerShell脚本放入Hexo具有特殊含义的scripts目录，都会导致看似严重的问题。今后遇到报错时，我会优先保留现场、记录命令、缩小问题范围，再做最小修改并复测；同时在修改系统配置前先备份文件，使排错过程可回退、可复盘。

## 7.3 安全责任与规范意识

账户与权限实验使我认识到，“能运行”并不是软件工程的唯一标准。直接开放777权限、将私钥或Token上传到仓库、长期使用root账户登录，虽然可能暂时省事，却会把局部问题转化为更大的安全风险。本实验采用webgroup共享组、2770目录权限、SSH公钥认证以及独立配置文件，体现了最小权限、敏感信息隔离和职责分离的基本原则。

自动化同样伴随着责任。部署脚本中的rsync --delete可以保持目标目录整洁，但如果源路径或目标路径写错，也可能快速删除重要文件；因此自动化流程必须配置明确的目录、错误中止机制和结果检查。作为软件工程专业学生，我需要对自己编写和执行的命令负责，不仅关注功能结果，还应重视数据安全、服务稳定、配置审计和故障回滚。

## 7.4 开源文化与国产软件生态

Hexo、Git、VS Code、VirtualBox、Ubuntu、OpenSSH和Nginx等工具，使个人能够以较低成本完成从内容开发到服务器发布的完整流程。我在使用这些工具的过程中切实体会到，开源并不只是“免费使用”，更重要的是公开文档、开放标准、社区协作和持续改进。许多问题都可以通过阅读官方文档、查看日志和参考社区经验解决，这种知识共享显著降低了学习和创新门槛。

与此同时，国内已经形成Gitee、Coding、openEuler、龙蜥等平台和操作系统生态。国产软件生态的发展既需要核心技术突破，也需要高质量文档、稳定工具链、开放社区和真实用户反馈。今后的学习中，我愿意从规范提交Issue、完善中文文档、参与开源项目测试等力所能及的工作做起，不只做工具的使用者，也逐步成为可靠的贡献者。

## 7.5 个人反思与后续学习计划

回顾整个实践过程，我从最初连node、npm和Git命令都需要逐项确认，逐步完成了虚拟机安装、Linux网络配置、用户权限管理、SSH免密登录、自建Git仓库和Nginx自动部署。这种进步让我意识到，技术能力往往不是一次性掌握的，而是在持续操作、犯错、验证和总结中形成。能够独立把一个静态站点部署到自己配置的Linux服务器，并通过脚本重复完成发布，是本次课程带给我最直观的成就感。

后续我计划在当前成果上继续学习Docker和GitHub Actions，将本次手动配置过的构建环境、服务配置和部署流程进一步容器化、自动化；同时补充Shell脚本、Linux网络、安全加固和CI/CD知识。更重要的是，我会延续本次实践形成的习惯：为项目维护README和版本记录，重要操作前备份，提交前检查敏感信息，部署后进行健康验证。只有把规范落实到每一次小操作中，才能逐步形成稳定、可信的软件工程能力。

# 8 技术展望：容器化与持续部署升级方案

## 8.1 代表性工具与替代选项

本报告选择“VirtualBox中的Ubuntu + 主机安装Nginx + PowerShell/SCP/rsync手工触发部署”作为代表性实践链路。该方案非常适合学习Linux、网络和服务器配置，但在多项目、多人协作和长期运行场景中，完整虚拟机占用资源较多，环境复制速度较慢，应用配置与操作系统耦合，部署仍依赖个人电脑触发。

| **现有工具/环节** | **替代或升级选项** | **适用特点** |
|:---|:---|:---|
| VirtualBox完整虚拟机 | Docker、Podman、WSL2、Hyper-V | 容器更轻量；WSL2适合本地Linux开发；Hyper-V适合Windows原生虚拟化 |
| Hexo | Hugo、VitePress、Astro、Jekyll | Hugo构建快；VitePress适合文档；Astro适合现代前端；Jekyll与GitHub Pages集成成熟 |
| Nginx | Caddy、Apache HTTP Server、GitHub Pages | Caddy自动HTTPS；Apache模块丰富；Pages免维护服务器 |
| PowerShell手动触发 | GitHub Actions、GitLab CI、Jenkins | 代码推送后自动构建、测试和部署 |
| SCP全量上传 | rsync、制品仓库、容器镜像仓库 | 增量传输或将可部署制品版本化 |

<img src=".//images/word-report/image48.png" style="width:6.29861in;height:3.14931in" />

图48 由虚拟机手工部署升级为容器化持续部署

## 8.2 升级目标

1/. 将Node.js、Hexo和Nginx运行环境写入Dockerfile或Compose文件，使构建和运行环境可复制。

2/. 以Git push作为唯一触发入口，由GitHub Actions自动安装依赖、生成站点、检查输出并构建镜像或发布制品。

3/. 使用GitHub Environment和Secrets保存服务器私钥、主机地址等敏感信息，不在仓库中明文保存。

4/. 部署前后执行健康检查，失败时自动停止发布或回滚到上一镜像/制品。

5/. 保留Podman作为无守护进程、可Rootless运行的替代容器引擎，降低长期守护进程和root权限依赖。

## 8.3 详细实施方案

### 8.3.1 第一阶段：构建环境容器化

采用多阶段Dockerfile。第一阶段使用Node.js LTS镜像安装npm依赖并执行npx hexo generate；第二阶段使用官方Nginx镜像，仅复制public目录和Nginx配置。构建镜像不包含源码、node_modules和私钥，最终运行层体积更小、攻击面更低。<sup>/[12/]</sup>

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;"># Stage 1: build<br />
FROM node:22-alpine AS builder<br />
WORKDIR /app<br />
COPY package*.json ./<br />
RUN npm ci<br />
COPY . .<br />
RUN npx hexo clean &amp;&amp; npx hexo generate<br />
<br />
# Stage 2: serve<br />
FROM nginx:alpine<br />
COPY nginx.conf /etc/nginx/conf.d/default.conf<br />
COPY --from=builder /app/public /usr/share/nginx/html<br />
EXPOSE 80<br />
HEALTHCHECK CMD wget -qO- http://localhost/ || exit 1</td>
</tr>
</tbody>
</table>

### 8.3.2 第二阶段：GitHub Actions持续集成

工作流在main分支推送时触发，依次检出代码、安装Node.js、执行npm ci、运行Hexo构建、检查public/index.html、构建容器镜像。Actions能够将构建过程固定在干净运行器中，避免“只在本人电脑上能运行”。<sup>/[13/]</sup>

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr>
<td style="text-align: left;">name: Build and Deploy<br />
on:<br />
push:<br />
branches: [main]<br />
jobs:<br />
build:<br />
runs-on: ubuntu-latest<br />
steps:<br />
- uses: actions/checkout@v4<br />
- uses: actions/setup-node@v4<br />
with:<br />
node-version: '22'<br />
cache: npm<br />
- run: npm ci<br />
- run: npx hexo clean &amp;&amp; npx hexo generate<br />
- run: test -f public/index.html<br />
- run: docker build -t software-tools-site:${{ github.sha }} .</td>
</tr>
</tbody>
</table>

### 8.3.3 第三阶段：安全部署与回滚

部署Job使用environment: production，敏感信息放入Secrets；通过SSH在服务器拉取指定提交对应的镜像并启动新容器。新容器先映射到临时端口进行健康检查，确认200后再切换Nginx反向代理或替换旧容器。保留上一版本镜像标签last-good，若健康检查失败则恢复旧容器。

- 分支保护：main需通过构建检查后才能合并。

- 并发控制：production同一时间只允许一个部署任务。

- 审批机制：关键环境可配置人工批准。

- 密钥管理：私钥、Token、服务器地址放入Secrets，日志中不输出敏感值。

- 回滚策略：镜像以Git SHA标记，保留last-good和最近若干版本。

- 健康检查：部署前test -f public/index.html，部署后curl --fail访问站点。

## 8.4 可行性、风险与收益

| **方面** | **预期收益** | **主要风险与控制** |
|:---|:---|:---|
| 环境一致性 | 开发、CI和服务器使用同一镜像定义 | 固定基础镜像版本；定期更新依赖并扫描漏洞 |
| 部署效率 | 推送后自动构建和发布，减少手工步骤 | 设置审批、并发限制和健康检查，防止错误自动扩散 |
| 回滚能力 | 镜像和Git SHA一一对应，可快速恢复 | 保留last-good标签和部署日志 |
| 资源占用 | 容器比完整虚拟机启动更快、占用更低 | 教学阶段仍保留VirtualBox以学习Linux与网络基础 |
| 安全性 | Secrets集中管理，容器可Rootless运行 | 最小权限、只读文件系统、限制网络与端口 |

## 8.5 网络调研方法

技术展望采用多源交叉验证：第一，阅读Docker、Podman、GitHub Actions、GitHub Pages和Nginx官方文档，确认工具定位、工作流语法与部署能力；第二，对比现有实验中的真实痛点，包括虚拟机资源占用、手工步骤、环境差异与回滚困难；第三，以最小可行原型设计Dockerfile和Actions流程，并用构建产物存在性、HTTP 200、Git SHA标签等可验证指标评估方案；第四，保留替代工具和风险分析，避免把单一工具描述为唯一答案。<sup>/[12-13/]</sup>

## 8.6 展望结论

VirtualBox+Ubuntu+Nginx是理解操作系统、网络和权限的必要学习路径，不应被容器完全替代；容器化和GitHub Actions则适合把已掌握的手工流程标准化、自动化。合理升级方式是“先用虚拟机理解底层，再用容器固化应用环境，最后用CI/CD控制交付”，从而兼顾基础能力与工程效率。

# 9 参考文献

/[1/] 杭州电子科技大学. 《软件开发工具实践》考核作业要求即任务书/[Z/]. 2026.

/[2/] 杭州电子科技大学. 《软件开发工具实践》实验参考手册/[Z/]. 2026.

/[3/] OpenJS Foundation. Node.js Documentation/[EB/OL/]. <https://nodejs.org/learn>，访问日期：2026-07-10.

/[4/] npm, Inc. npm CLI Documentation: Config and Registry/[EB/OL/]. <https://docs.npmjs.com/cli/v11/using-npm/config/>，访问日期：2026-07-10.

/[5/] Hexo. Hexo Documentation/[EB/OL/]. <https://hexo.io/docs/>，访问日期：2026-07-10.

/[6/] Software Freedom Conservancy. Git Reference/[EB/OL/]. <https://git-scm.com/docs>，访问日期：2026-07-10.

/[7/] Oracle. Oracle VirtualBox Documentation/[EB/OL/]. <https://docs.oracle.com/en/virtualization/virtualbox/>，访问日期：2026-07-10.

/[8/] Canonical. Ubuntu Server Documentation/[EB/OL/]. <https://ubuntu.com/server/docs/>，访问日期：2026-07-10.

/[9/] Canonical. Netplan Documentation: How to use static IP addresses/[EB/OL/]. <https://netplan.readthedocs.io/en/stable/using-static-ip-addresses/>，访问日期：2026-07-10.

/[10/] Canonical. OpenSSH Server on Ubuntu/[EB/OL/]. <https://ubuntu.com/server/docs/how-to/security/openssh-server/>，访问日期：2026-07-10.

/[11/] F5, Inc. nginx Documentation/[EB/OL/]. <https://nginx.org/en/docs/>，访问日期：2026-07-10.

/[12/] Docker, Inc. Docker Documentation: Get Started/[EB/OL/]. <https://docs.docker.com/get-started/>，访问日期：2026-07-10.

/[13/] GitHub, Inc. GitHub Actions Documentation/[EB/OL/]. <https://docs.github.com/actions>，访问日期：2026-07-10.

